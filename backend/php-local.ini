; Limites locales pour les médias (image / vidéo jusqu'à 200 Mo).
; Lancer le backend avec : php -c php-local.ini artisan serve

upload_max_filesize = 200M
post_max_size = 220M
memory_limit = 512M
max_execution_time = 600
max_input_time = 600
